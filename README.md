# Dublin Disaster Response Management System 

## Steps to run the code:
- clone the repository
- install the required dependencies: `npm install --legacy-peer-deps`
- start the node server: `npm run start`
